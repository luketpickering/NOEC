syntax = "proto3";

package noec;

message Command {

  enum command {
    START = 0;
    END = 1;
    UPDATE = 2;
  };

  command cmd = 1;
}

message UpdateHeader {
 int32 numbytes = 1;
}

message UpdateBody {
 map<int32, int32> values = 1;
}
